# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
以下のようなhidddenフィールドが出力される。
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
todosテーブルの主キーである、idカラムの値であるID。

### findメソッドで実行しているSQLは何か
例えば ID が 1 の場合、次のようなSQLが実行される。
select * from todos where id = 2 limit 1;

### findメソッドで取得できる値は何か
返り値は Todoモデルのインスタンス。
取得できなかった場合は null を返す。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
DBにまだ保存されていなければ新しいレコードとしてINSERTに切り替える。DBにすでに保存されていればUPDATEに切り替える。


## Todo論理削除

### traitとclassの違いとは

trait：1つのクラスに複数適用できる。インスタンス化はできない。しようとするとエラーが起こる。
class：継承は1つのみ可能。インスタンス化ができる。



### traitを使用するメリットとは
複数クラス間でコードを共通化・再利用でき、継承関係を複雑にせず機能を追加できる。


## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
ルートファイルでTodoControllerのメソッドが呼び出されたとき、LaravelのサービスコンテナにてTodoControllerクラスがインスタンス化されるタイミングで実行される。

### RequestクラスからFormRequestクラスに変更した理由
バリデーションルールをコントローラから分離し、コードを整理するため。また、authorize() による認可チェックも追加するため。

### $errorsのhasメソッドの引数・返り値は何か
引数　入力欄のname属性
返り値　boolean（指定フィールドにエラーがあるかどうかを判定するため）


### $errorsのfirstメソッドの引数・返り値は何か
引数　入力欄のname属性
返り値　文字列（その入力欄で最初に発生したエラーメッセージを返すため）


### フレームワークとは何か
ソフトウェア開発における「枠組み」のことを意味する。
容易に一定品質のプロダクトを作成することができる。
枠組みが決定しているため初見のアプリケーションでもコードの記述場所などが特定しやすい。


### MVCはどういったアーキテクチャか
開発効率を高めるために作られたアーキテクチャ。
再利用性・可読性を優れ、保守性を高めてくれる。
Model・View・Controllerごとに役割が分担されている。


### ORMとは何か、またLaravelが使用しているORMは何か
オブジェクトとデータベースのテーブルを関連付けする。SQLを直接操作することなく、データベースとマッピングされたClassのメソッドを用いることでDBとやり取りを行うことができる。

Laravelが使用しているORMは、Eloquentという。


### composer.json, composer.lockとは何か
composer.jsonは、依存ライブラリの宣言ファイル。
composer.lockは、実際にインストールされたライブラリのバージョンを記録するファイル。
どちらもアプリ開発に必要なライブラリを管理し、開発チーム全員が、限りなく同じ環境で作業できるようにするために必要なファイルである。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendorディレクトリ